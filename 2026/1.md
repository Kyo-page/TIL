# 2026年　1月

2026年1月の記録です。

## 1/19

TILを始めた。

**TIL のメリット・特徴**

- 1日1つ、学んだことを**短く**記録すればOK  
- Markdownで書けて、**コードがなくてもコミット可能**
- 学習量が少ない日でも続けやすく、**習慣化しやすい**
- 書くことで理解が整理され、**知識が定着しやすい**
- GitHubの草が**自然に生える**
- 学習の積み重ねが残り、**成長を振り返れる**
- 継続力・学習姿勢のアピールになり、**ポートフォリオ補助にも使える**

これを習慣化することでGitHubに慣れていきたい。

## 1/20

- StackBlitzで作成していた練習用ポートフォリオサイトを、ローカル環境にcloneして動作確認・テスト編集を行った。
- GitHubの操作に慣れるため、実務を想定して **issue作成 → 作業ブランチ作成 → 実装 → PR作成 → Squash & merge** まで一連の流れを体験した。
- ポートフォリオサイトの修正やリファクタリングを行った。
  - コンポーネントの分割
  - JS の条件式を使った条件付きレンダリングでメニューボタン作成
  - 繰り返しの部分をmapを使ったリスト表示でリファクタリング
  - children propsを使ってリファクタリング
- GitHub Pagesに公開した。

今後はローカル環境を中心に開発し、GitHubを使ったブランチ運用・PRベースの開発フローを継続していきたい。

## 1/23

[ランダム名言ジェネレータ](https://kyo-page.github.io/random-quotes-generator/)を作成してuseEffect でのデータフェッチについて学んだ。

### 1. useEffect によるデータフェッチとクリーンアップ
`useEffect` で API 通信を行う際は、メモリリークや意図しない挙動を防ぐための「お掃除（クリーンアップ）」が必須。

- **クリーンアップ関数**: コンポーネントが消える際に `active = false` とすることで、未完了の通信結果が破棄されたコンポーネントの状態を更新するのを防ぐ。
- **Promise の 3 ルート**:
  - `.then()`: 成功時の処理（データのセット）。
  - `.catch()`: 失敗時の処理（エラー情報の保持）。
  - `.finally()`: 共通処理（ローディングフラグのオフ）。

### 2. ユーザー体験を向上させる条件付きレンダリング
三項演算子を使用して、通信状態に応じた UI を提供する。
- **Loading**: スピナーを表示し、処理中であることを伝える。
- **Error**: エラー内容を表示し、「再試行」ボタンを提供することで離脱を防ぐ。
- **Success**: 正常に取得したデータを安全（`?.` オプショナルチェーン）に表示する。

### 3. 実務におけるデータフェッチのあり方
`useEffect` でのフェッチは学習用としては良いが、実務では以下の理由から **React Query (TanStack Query)** や **SWR** などのライブラリが推奨される。
- **キャッシュ管理**: 一度取得したデータを再利用できる。
- **レースコンディション防止**: 古いリクエストが新しいデータを上書きするのを自動で防ぐ。
- **ボイラープレートの削減**: `loading` や `error` の管理をライブラリに一任できる。

## 1/24 
Reactチュートリアルの新たなチャプター「シンプルメモアプリ」を開始した。

今回からTypeScriptを使っていく。（使いながら覚える。）

また、今まではDaisyUIを使っていたが、今回はshadcn/uiを使ってみる。

環境構築中にエディタのインデントが赤くなる現象が起きたが、フォルダを開き直したら直った。

今日はレイアウトの骨組み作りまでできた。

### 最近の振り返り

- web制作についてのxアカウントの作成
- GitHubの基本的な使い方の把握・実践
- TILの習慣を開始した
- 目標設定（Will/Can/Must）

### 新たに得た学び

- 学習を習慣づけるためのコツ
- 開発の中でのGitHubの運用フロー

### 感想や今後の改善点

- TILを継続したり毎日何らかのコードを書いたりして草を絶やさないようにしたい
- 学習用に制作するアプリでもGitHubを活用していきたい

### 今後やりたいこと
- [ ] 既存アプリのUIをFigmaでトレース→Reactで再現してみる
- [ ] UIライブラリをいろいろ試してみる
- [ ] AIエージェントの活用方法を探る

## 1/25
「シンプルメモアプリ」の解説動画を見ながらコードを書き写していった。

ただ写すだけでなく、
- 1つ1つ意味しながらコメントで注釈をつける
- shadcn/uiを組み込む

を実施した。

今日はメモの追加機能の実装までできた。

### 感想
- 入力欄のコンポーネントとメモ追加の関数を分けるやり方がとても参考になった。
- TypeScriptに手こずったが、なんとかついていけそう。

## 1/26
Figma Makeを使ってバイブコーディングをやってみた。

出力されたコードに問題があったり、GitHubPagesにデプロイする際にエラーが出たりしたが、CursorのchatやGeminiに修正してもらいながら解決した。

[https://kyo-page.github.io/portfolio/](https://kyo-page.github.io/portfolio/)

## 1/27
**三項演算子を使うときは、必ず「<タグ>中身</タグ>」がセットになって完結している必要がある！！**

これが分かってなかったせいで、かなりの時間を消費してしましった。

## 1/28
「シンプルメモアプリ」のチュートリアルを進めた。

[疑問リスト](misc/learning-flow.md)の運用を始めたので、途中つまづいた部分を記録しながら進めた。

あとで見返した時に、
- 何がわからなかったのか
- どうやって解決したか

が振り返れて効率的だと思った。

### 今日解決した疑問
- 「配列」と「オブジェクト」の見分け方
- onDelete: (id: number) => void;って何？
- 何も返さない関数って？

## 1/29
「シンプルメモアプリ」のチュートリアルを完了した。
### 振り返り
- 高度なState管理と不変性の制御
  - スプレッド構文を用いた、ネストした配列・オブジェクトの正確なイミュータブル操作。
- Reactの本質的な理解と最適化
  - useStateと再レンダリングの仕組みを捉えた、低負荷で堅牢なコンポーネント設計。
- 型安全な実装（TypeScript）
  - TypeScriptを導入し、開発効率と保守性を両立させたバグの少ないコード構築。
### 感想
理解が不十分な部分があったと感じたので、次のチュートリアルに進む前に一旦立ち止まって以下の復習を実施しようと思った。
- TypeScriptについて
- コンポーネントの階層構造やpropsの受け渡しについて
- Immerの使い方
